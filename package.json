{
  "name": "invoice-generator",
  "version": "1.0.0",
  "description": "Full-stack MERN Invoice Generator",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "private": true
}
